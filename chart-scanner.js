(()=>{const $=s=>document.querySelector(s);const btn=$('#uploadBtn'),input=$('#chartInput'),card=document.querySelector('.scanner-card');if(!btn||!input||!card)return;let preview=null;function toast(m){const t=$('#toast');if(t){t.textContent=m;t.classList.add('show');clearTimeout(window.__scanToast);window.__scanToast=setTimeout(()=>t.classList.remove('show'),2600)}}function render(file){if(preview)preview.remove();preview=document.createElement('div');preview.style.cssText='margin-top:16px;padding:14px;border:1px solid rgba(140,110,255,.35);border-radius:16px;background:#08111f';const img=document.createElement('img');img.alt='Uploaded chart preview';img.style.cssText='display:block;width:100%;max-height:420px;object-fit:contain;border-radius:12px;background:#020711';img.src=URL.createObjectURL(file);const row=document.createElement('div');row.style.cssText='display:flex;gap:10px;align-items:center;justify-content:space-between;margin-top:12px;flex-wrap:wrap';const name=document.createElement('span');name.textContent=`${file.name} · ${(file.size/1024/1024).toFixed(2)} MB`;name.style.cssText='color:#aeb9cf;font-size:13px';const analyze=document.createElement('button');analyze.type='button';analyze.textContent='Analyze uploaded chart';analyze.className='upload-btn';analyze.style.cssText='cursor:pointer';analyze.onclick=()=>{toast('Chart loaded. Vision analysis will use this image when the AI vision endpoint is connected.');};const remove=document.createElement('button');remove.type='button';remove.textContent='Remove';remove.className='text-btn';remove.onclick=()=>{URL.revokeObjectURL(img.src);preview.remove();preview=null;input.value='';toast('Chart removed.')};row.append(name,analyze,remove);preview.append(img,row);card.appendChild(preview);toast('Chart uploaded successfully.')}btn.addEventListener('click',e=>{e.preventDefault();input.click()});input.addEventListener('change',e=>{const file=e.target.files?.[0];if(!file)return;if(!file.type.startsWith('image/')){toast('Please select a chart image.');input.value='';return}if(file.size>10*1024*1024){toast('Chart image must be under 10 MB.');input.value='';return}render(file)})})();